import { VisibleDirectiveDirective } from './visible-directive.directive';

describe('VisibleDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibleDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
